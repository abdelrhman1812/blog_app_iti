const endPoints = {
  /* Posts */
  posts: "/posts",
  addPosts: "/posts",
  deletePosts: "/posts",
  patchPosts: "/posts",
  /* User */
  register: "/auth/signup",
  login: "/auth/signin",
  userProfile: "/users",
};

export default endPoints;
