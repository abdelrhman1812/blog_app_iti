const endPoints = {
  /* Posts */
  posts: "/posts",
  addPosts: "/posts",
  deletePosts: "/posts",
  patchPosts: "/posts",
  /* comments */
  comments: "/posts",

  /* User */
  register: "/auth/signup",
  login: "/auth/signin",
  userProfile: "/users",
};

export default endPoints;
